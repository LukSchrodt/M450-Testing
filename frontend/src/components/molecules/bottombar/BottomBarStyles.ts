import {SxProps} from "@mui/material";

const bottomBar : SxProps = {
    top: 54,
    left: 0,
    right: 0,
    minHeight: "52px",
    width: "100%",
    flex: 1,
    position: "fixed",
};

export default {bottomBar};